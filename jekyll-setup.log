##### TODO: remove this file before merging back to development!

# gem install jekyll bundler
$ bundle init

$ vi Gemfile
    -> add: gem "jekyll"

$ bundle config set --local path vendor/bundle
$ bundle install
    -> will install everything into the local directory
    -> will create a Gemfile.lock, which is to be checked into VCS
